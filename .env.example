# OpenHamster local secrets and runtime overrides
# Copy to `.env.local` for machine-specific values. Do not commit real keys.

MINIMAX_API_KEY=
FRED_API_KEY=

# Optional runtime defaults
LLM_PROVIDER=minimax
LLM_MODEL=MiniMax-M2.5
