# GobyShrimp local override
# [Source 来源] developer machine override
# [Purpose 用途] keep only local-only provider choices that differ from repository defaults

# [Source 来源] local.yaml
# [Purpose 用途] local development defaults for faster iteration and fewer external dependencies
data_source:
  provider: stooq
  fallback:
    - akshare
    - yfinance

events:
  expected_sync_interval_minutes: 5
